/// dev app main route with context provider and project switcher integration

'use client';

import { De V ModuleProvider } from '@/contexts';
export default function DevLayout({ children }) {
 return (
<DeVModule.>

{children}

</Dev module>
)
} // TODO: Integrate Supabase providers in root layout