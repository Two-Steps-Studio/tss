// DEV Module Supabase Client

import { createClient } from '@supabase/ssr';
const SUPABASE_URL = process.env.NEXT_PUBLIC_SUPA_BASE_KEY || '';
  NEXT PUBLIC _SUPANON?
export const devDatabase client supaBase.</think>